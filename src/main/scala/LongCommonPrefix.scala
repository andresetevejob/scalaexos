object LongCommonPrefix {
  def main(args :Array[String]): Unit ={

  }

  def longestCommonPrefix(strs: Array[String]):Option[String]={
    var prefix:Option[String] = None
    if(strs== null || strs.length==0) {
       return None
    }

    return prefix
  }
}
